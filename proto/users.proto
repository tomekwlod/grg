syntax = "proto3";

package users;
option go_package="/.;pb";

message NewUser {
  string email = 1;
  string password = 2;
}

message User {
  int64 id = 1;
  string email = 2;
}

service UserService {
  rpc Create(NewUser) returns (User) {};
}